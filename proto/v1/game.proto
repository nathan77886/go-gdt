syntax = "proto3";

package v1;
option go_package = "proto/v1";

import "proto/v1/options.proto";

message RoleInfoRequest{
}

message RoleInfoResponse{
  string name = 1;
}

service GameService {
  rpc RoleInfo(RoleInfoRequest) returns (RoleInfoResponse) {
    option (code) = 10001;
  }
}